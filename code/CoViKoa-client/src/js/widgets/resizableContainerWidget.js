import { Widget, DockPanel, SplitPanel } from '@lumino/widgets';

/**
* This is the container widget that will host components
* when resizing is permitted and when there is
* multiple components.
* Widgets will be attached to this container, will have a little tabBar with their
* names, and will be movables / resizables (DockPanel + SplitPanel behavior)
*
*/
export default class ResizableSplitContainer extends Widget {
  
}
