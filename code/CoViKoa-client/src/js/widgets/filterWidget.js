import { Widget } from '@lumino/widgets';

/**
* This is the widget that allows to filter the features displayed on
* a MapWidget.
* This corresponds to component defined as gviz:FilterComponent in CoViKoa graph.
*
*/
export default class FilterWidget extends Widget {

}
