import { Widget, DockPanel, SplitPanel, BoxPanel } from '@lumino/widgets';

/**
* This is the container widget that will host components
* when resizing is not permitted and/or when there is
* only one MapWidget (+ optionally a StaticWidget)
*
*
*/
export default class FixedContainer extends Widget {

}
